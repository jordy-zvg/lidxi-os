import { PageStub } from '@lidxi/ui';

/**
 * Home del OMS — Kanban de pedidos en vivo.
 *
 * Esta pantalla se va a implementar a partir del mockup HTML que el cliente
 * proveerá. Por ahora deja un placeholder; ver issue de Home Kanban para el
 * scope completo (columnas Recibido / En preparación / Listo / En camino,
 * tarjetas con SLA color-coded, realtime vía Supabase, acciones inline).
 */
export default function PedidosPage() {
  return <PageStub title="Pedidos — Kanban en vivo" description="Pendiente de mockup HTML." />;
}
